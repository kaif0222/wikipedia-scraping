# wikipedia-scraping
This project demonstrates web scraping in Python using two approaches:  Requests + pandas – directly reading HTML tables into DataFrames.  Requests + BeautifulSoup – parsing HTML manually to extract table data.  It scrapes a table of countries and populations from Wikipedia and saves it as a CSV file
